{
  "name": "@cuidly/database",
  "version": "1.0.0",
  "private": true,
  "main": "./client.ts",
  "scripts": {
    "build": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "generate": "prisma generate",
    "seed": "ts-node prisma/seed.ts"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.26",
    "dotenv": "^16.4.5",
    "prisma": "^7.1.0"
  }
}